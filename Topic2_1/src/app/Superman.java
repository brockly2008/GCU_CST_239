package app;

// Class representing Superman, a specific type of SuperHero
public class Superman extends SuperHero {
	
    // Constructor to create a Superman instance with a specific health value
    public Superman(int health) {
        super("Superman", health); // Call the SuperHero constructor with the name "Superman" and given health
    }
}
